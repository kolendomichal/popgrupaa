const baseUrl = "http://localhost:5000/"

const taskUrl = `${baseUrl}task/` // plus app
const APP_URL = `${baseUrl}application/`
const NODE_URL = `${baseUrl}nodes/`
const clusterUrl = `${baseUrl}nodes/`
const machinesUrl = `${baseUrl}machines/`

export{
    NODE_URL,
    taskUrl,
    APP_URL,
    clusterUrl,
    machinesUrl
}