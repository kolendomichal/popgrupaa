const INITIAL_STATE = {
    list: []
};

const clusterMachineListReducer = (state = INITIAL_STATE, action) => {
    switch(action.type) {
        default : return state;
    }
};

export default clusterMachineListReducer;